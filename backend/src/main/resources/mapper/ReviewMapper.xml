<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whlg.hospital.mapper.ReviewMapper">

    <resultMap id="BaseResultMap" type="com.whlg.hospital.entity.Review">
        <id column="id" property="id"/>
        <result column="order_type" property="orderType"/>
        <result column="order_id" property="orderId"/>
        <result column="user_id" property="userId"/>
        <result column="doctor_id" property="doctorId"/>
        <result column="rating" property="rating"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_type, order_id, user_id, doctor_id, rating, content, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select <include refid="Base_Column_List"/> from t_review where id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from t_review order by id desc
    </select>

    <insert id="insert" parameterType="com.whlg.hospital.entity.Review" useGeneratedKeys="true" keyProperty="id">
        insert into t_review (
            order_type, order_id, user_id, doctor_id, rating, content, create_time
        ) values (
            #{orderType}, #{orderId}, #{userId}, #{doctorId}, #{rating}, #{content}, #{createTime}
        )
    </insert>

    <update id="updateById">
        update t_review
        set order_type = #{et.orderType},
            order_id = #{et.orderId},
            user_id = #{et.userId},
            doctor_id = #{et.doctorId},
            rating = #{et.rating},
            content = #{et.content},
            create_time = #{et.createTime}
        where id = #{et.id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        delete from t_review where id = #{id}
    </delete>

</mapper>